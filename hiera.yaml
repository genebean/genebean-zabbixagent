---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:

  - name: "Operating System"
    path: "%{facts.os.name}.yaml"

  - name: "OS Family"
    path: "%{facts.os.family}.yaml"

  - name: "Kernel"
    path: "%{facts.kernel}.yaml"

  - name: "Defaults"
    path: "common.yaml"
