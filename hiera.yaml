---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:

  - name: "Operating System"
    path: "%{operatingsystem}.yaml"

  - name: "OS Family"
    path: "%{osfamily}.yaml"

  - name: "Kernel"
    path: "%{kernel}.yaml"

  - name: "Defaults"
    path: "common.yaml"